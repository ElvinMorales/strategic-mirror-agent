# Prompt Surface

Use `system.md` as the primary system prompt surface. Use task prompts in `tasks/` for focused behavior.

The prompt stack should load direction, Memory, State, Knowledge, connector registry, Guardrails, and Workflow before acting when available.
