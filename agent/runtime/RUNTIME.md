# Runtime

The public repo is a template, not a live runtime.

A private runtime instance may copy templates into a private folder or repository. That instance may contain real Memory, State, Knowledge, connector configs, outputs, and local environment files.

The public scaffold must remain free of private context and live connector credentials.
